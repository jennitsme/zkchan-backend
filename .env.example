PORT=8080
APP_NAME=zkChan Backend

# Allowed origins are comma-separated. Put ONLY your production origin here.
# You can temporarily include http://localhost:5500 for local testing.
CORS_ORIGINS=https://zk-chan.fun

# Optional: log level "dev" | "combined"
LOG_FORMAT=dev
SESSION_TTL_SECONDS=900
PROOF_TTL_SECONDS=1800
